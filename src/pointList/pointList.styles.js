import React from 'react';
import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        width: '100%',
    },
    card: {
        height: 400,
        marginTop: 20,
        padding: 10,
    },
    map: {
        width: '100%',
        height: '80%',
    },
});
